<Page
    x:Class="IRCA.sharePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:IRCA"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Rectangle Fill="gray"/>
        <StackPanel>
            <TextBlock Text="Please copy the path and open in folder. All the file is well design to move to other devices."
                       Foreground="White"
                       Style="{StaticResource HeaderTextBlockStyle}"
                       Margin="35"
                       HorizontalAlignment="Left"/>
            <Grid Margin="35">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Button x:ConnectionId='1' Width="45" 
                        Foreground="White"
                        Background="Red"
                        Content="Copy"
                        Margin="15"
                        VerticalAlignment="Center" 
                        FocusVisualPrimaryBrush="#FF454545"
                                              />
                <TextBox x:ConnectionId='2' Name="FolderPath"
                         Grid.Column="1"
                         Margin="0, 35, 35, 35" 
                         Foreground="White"
                         HorizontalAlignment="Stretch"
                         TextWrapping="Wrap"
                                                   />
            </Grid>
        </StackPanel>
    </Grid>
</Page>

